const { writeToJSONFile, readFromJSONFile } = require(`../fileHandler`);

test(`Test writing to JSON file`, () => {
//Test writeToJSONFile function...
});

test(`Test reading from JSON file`, () => {
    //test readFromJSONFile function...
});